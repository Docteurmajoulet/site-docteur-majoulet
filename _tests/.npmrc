# TECH19AT-2026-09-15 : aucun script d'installation de dépendance (le paquet vnu-jar irait télécharger un Java 17 s'il en manquait un ;
# fsevents, optionnel et macOS seulement, embarque déjà son binaire). npm run / npm test sont inchangés.
ignore-scripts=true
